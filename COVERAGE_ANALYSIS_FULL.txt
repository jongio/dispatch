# Test Coverage Analysis: internal/data Package

**Current Coverage: 61.4% of statements**

## EXECUTIVE SUMMARY

The internal/data package has solid coverage (61.4%) for core database query functions, but is missing coverage for:
- **Maintenance functions** (LastReindexTime, Maintain error paths)
- **Chronicle reindex orchestration** (ChronicleReindex - 168 LOC)
- **Binary/PTY discovery** (findCopilotBinary, startPTY - ~120 LOC)
- **Helper functions** (isDBBusy - rarely tested directly)

---

## EXPORTED FUNCTIONS STATUS

| Function | Lines | Status | Tests | Coverage Notes |
|----------|-------|--------|-------|-----------------|
| Open() | 39-45 | ✅ Tested | TestCovOpen_WithDispatchDBOverride | Calls platform.SessionStorePath() → OpenPath() |
| OpenPath() | 47-62 | ✅ Tested | TestOpenPathValidFile, TestOpenPathMissingFile | File exists check, read-only connect, Ping |
| Close() | 65-67 | ✅ Tested | TestCloseMultipleTimes, TestCovStoreClose | Simple db.Close() wrapper |
| Maintain() | 77-122 | ⚠️ Partial | TestCovMaintain_NoStoreFile, TestCovMaintain_ValidStore | **Missing:** isDBBusy error paths (3x) |
| LastReindexTime() | 127-140 | ❌ None | — | **Untested:** 4 branches, home dir error |
| ListSessions() | 320-351 | ✅ Tested | 15+ test functions | Full coverage |
| ListSessionsByIDs() | 355-396 | ✅ Tested | TestCovListSessionsByIDs_* | Full coverage |
| GetSession() | 400-489 | ✅ Tested | 10+ test functions | Full coverage |
| SearchSessions() | 494-535 | ✅ Tested | 10+ test functions | Full coverage |
| ListFolders() | 539-541 | ✅ Tested | TestListFolders | Full coverage |
| ListRepositories() | 545-547 | ✅ Tested | TestListRepositories | Full coverage |
| ListBranches() | 551-558 | ✅ Tested | TestListBranches_* | Full coverage |
| GroupSessions() | 562-612 | ✅ Tested | 4x pivot types + filters | Full coverage |

---

## UNTESTED FUNCTIONS (Complete List)

### 1. LastReindexTime() - Line 127-140
**Type:** Public, Package-level  
**Size:** 13 LOC  
**Why Untested:** Requires filesystem mocking  
**Testable Paths:**
- WAL file exists with size > 0 → Return WAL mtime (line 133-134)
- WAL file exists with size == 0 → Check DB file (line 135-137)
- WAL doesn't exist → Return DB mtime (line 136-137)
- DB doesn't exist → Return zero time (line 139)
- os.UserHomeDir() error → Return zero time (line 129-130)

**Test Implementation:**
`go
func TestLastReindexTime_WALExists(t *testing.T) {
    // Mock os.Stat to return WAL file with mtime
    // Call LastReindexTime()
    // Verify returns that mtime
}
// Similar tests for other branches
`

### 2. Maintain() - Error Paths - Line 94-95, 104-105, 114-115
**Type:** Public, Package-level  
**Size:** 3 error handling blocks  
**Why Untested:** Requires database to return "locked" error  
**Untested Paths:**
- Line 94-95: WAL checkpoint returns isDBBusy error → Return ErrIndexBusy
- Line 104-105: FTS5 rebuild returns isDBBusy error → Return ErrIndexBusy
- Line 114-115: FTS5 optimize returns isDBBusy error → Return ErrIndexBusy

**Test Implementation:**
`go
func TestMaintain_DBLockedOnWALCheckpoint(t *testing.T) {
    // Create store with mocked db that returns "database is locked"
    // Call Maintain()
    // Verify returns ErrIndexBusy
}
`

### 3. ChronicleReindex() - Line 77-244 (Complete Function)
**Type:** Public, Package-level  
**Size:** 168 LOC  
**Why Untested:** Requires PTY mocking and Copilot CLI binary  
**Untested Code Paths:**

| Lines | Path | Testable | Implementation |
|-------|------|----------|-----------------|
| 78-80 | findCopilotBinary() returns "" | ✅ Yes | Mock function to return "" |
| 83-86 | startPTY() error | ✅ Yes | Mock function to return error |
| 177-179 | Context.Done() during collect | ✅ Yes | Cancel context in mock PTY |
| 182-183 | PTY closed before collecting | ✅ Yes | Mock PTY to close immediately |
| 186-188 | Early startup detection (5 lines) | ✅ Yes | Mock PTY to emit 5 lines |
| 210 | /experimental on write error | ✅ Yes | Mock Write to fail |
| 220 | /chronicle reindex write error | ✅ Yes | Mock Write to fail |
| 228 | /exit write (already best-effort) | ✅ Partial | Current: //nolint unchecked |
| 235-243 | Success detection (reindexed/sessions/error) | ✅ Yes | Mock PTY output variations |

**Test Implementation Strategy:**
`go
type mockPTY struct {
    readCh chan string
    // controls which phase to test
}

func TestChronicleReindex_NoBinary(t *testing.T) {
    // Mock findCopilotBinary to return ""
    // Call ChronicleReindex
    // Verify returns ErrCopilotNotFound
}

func TestChronicleReindex_StartPTYError(t *testing.T) {
    // Mock startPTY to return error
    // Verify error propagates
}

func TestChronicleReindex_ContextCancelled(t *testing.T) {
    // Mock PTY, cancel context during collect
    // Verify ErrReindexCancelled returned
}

func TestChronicleReindex_SuccessWithReindexedMarker(t *testing.T) {
    // Mock PTY to emit "reindexed" in output
    // Verify returns nil
}

func TestChronicleReindex_SuccessWithSessionsMarker(t *testing.T) {
    // Mock PTY to emit "sessions" in output
    // Verify returns nil
}

func TestChronicleReindex_FailureWithErrorMarker(t *testing.T) {
    // Mock PTY to emit "error" in output
    // Verify returns "chronicle reindex reported an error"
}
`

### 4. findCopilotBinary() (Unix) - Line 43-88
**Type:** Unexported, Platform-specific  
**Size:** 45 LOC  
**Why Untested:** Requires PATH manipulation and filesystem mocking  
**Untested Paths:**

| Lines | Path | OS | Condition |
|-------|------|----|---------  |
| 45-46 | exec.LookPath("copilot") | Any | Finds copilot in PATH |
| 53-66 | Platform detection | Darwin | GOARCH == arm64 → copilot-darwin-arm64 |
| 53-66 | Platform detection | Darwin | GOARCH != arm64 → copilot-darwin-x64 |
| 53-66 | Platform detection | Linux | GOARCH == arm64 → copilot-linux-arm64 |
| 53-66 | Platform detection | Linux | GOARCH != arm64 → copilot-linux-x64 |
| 68-70 | Unknown platform | Other | Returns "" (binaryDir == "") |
| 73-86 | Home + candidates search | Darwin/Linux | Finds in ~/.npm-global |
| 73-86 | Home + candidates search | Darwin/Linux | Finds in /usr/local/lib |
| 73-86 | Home + candidates search | Darwin/Linux | Finds in /usr/lib |
| 87 | No candidate found | Darwin/Linux | Returns "" |

**Test Implementation:**
`go
func TestFindCopilotBinary_InPATH(t *testing.T) {
    // Mock exec.LookPath to return path
    // Verify returns that path
}

func TestFindCopilotBinary_DarwinARM64(t *testing.T) {
    // Mock runtime.GOOS="darwin", GOARCH="arm64"
    // Mock os.Stat for candidates
    // Verify searches copilot-darwin-arm64 directory
}

func TestFindCopilotBinary_LinuxX64(t *testing.T) {
    // Mock runtime.GOOS="linux", GOARCH!="arm64"
    // Verify searches copilot-linux-x64 directory
}

func TestFindCopilotBinary_UnknownPlatform(t *testing.T) {
    // Mock runtime.GOOS="unknown"
    // Verify returns "" early
}

func TestFindCopilotBinary_NotFound(t *testing.T) {
    // Mock all candidates to not exist (os.Stat returns error)
    // Verify returns ""
}
`

### 5. findCopilotBinary() (Windows) - Line 41-58
**Type:** Unexported, Platform-specific  
**Size:** 18 LOC  
**Why Untested:** Requires environment variable and filesystem mocking  
**Untested Paths:**

| Lines | Condition |
|-------|-----------|
| 43-44 | ProgramFiles env var set, candidate exists |
| 43-44 | ProgramFiles env var not set |
| 46-50 | APPDATA set, candidate exists |
| 46-50 | APPDATA not set |
| 52-56 | Candidate found in loop |
| 57 | No candidate found |

### 6. startPTY() (Unix) - Line 33-40
**Type:** Unexported, Platform-specific  
**Size:** 7 LOC  
**Untested Paths:**
- Line 35: pty.StartWithSize succeeds
- Line 36-37: pty.StartWithSize returns error

### 7. startPTY() (Windows) - Line 31-38
**Type:** Unexported, Platform-specific  
**Size:** 7 LOC  
**Untested Paths:**
- Line 33: conpty.Start succeeds
- Line 34-35: conpty.Start returns error

### 8. ptyHandle Methods (Both Platforms)
**Type:** Unexported, PTY wrappers  
**Size:** 1 LOC each (6 total)
- Read() - delegates to ptmx.Read() or cpty.Read()
- Write() - delegates to ptmx.Write() or cpty.Write()
- Close() - delegates to ptmx.Close() or cpty.Close()

---

## PARTIALLY TESTED FUNCTIONS

### 1. isDBBusy() - Line 144-153
**Status:** Indirectly tested through Maintain()  
**Direct Tests:** None  
**Recommendation:** Add explicit unit tests
`go
func TestIsDBBusy(t *testing.T) {
    tests := []struct{
        err error
        want bool
    }{
        {errors.New("database is locked"), true},
        {errors.New("database table is locked"), true},
        {errors.New("busy"), true},
        {errors.New("locked"), true},
        {errors.New("other error"), false},
        {nil, false},
    }
}
`

### 2. scanSession() - Line 304-312
**Status:** Indirectly tested through all query methods  
**Direct Error Tests:** Partially in GetSession_*ScanError tests  
**Covered:** Normal scanning, accessing all fields  
**Not covered:** Explicit Scan() error injection (though done via closed DB)

---

## MODELS.GO COVERAGE

All exports are **data structures or constants** — no behavior to test:
- ✅ Session, Turn, Checkpoint, SessionFile, SessionRef, SearchResult
- ✅ FilterOptions, SortOptions, SortField, SortOrder
- ✅ PivotField constants, SessionGroup, SessionDetail

**Status:** Fully tested through field access tests in coverage_test.go

---

## COVERAGE IMPACT ESTIMATE

### To reach 70% coverage:
- Add **LastReindexTime() tests** (13 LOC) = +1-2%
- Add **Maintain() error tests** (3 error paths) = +0.5%
- Add **isDBBusy() unit tests** (5 variants) = +0.3%
- **Total so far:** ~2-3% gain

### To reach 80% coverage:
- Add **ChronicleReindex() tests** (168 LOC) = +5-7%
- Add **findCopilotBinary() tests** (63 LOC) = +2-3%
- Add **startPTY() tests** (14 LOC) = +0.5%
- **Total from 61.4%:** +7-10% = ~69-71%

**Recommendation:** Focus on ChronicleReindex() first (highest impact), use mock PTY interface.

---

## QUICK FIX PRIORITY LIST

| Priority | Item | LOC | Effort | Impact | Testable Without CLI |
|----------|------|-----|--------|--------|-------------------|
| 1 | LastReindexTime() | 13 | 30m | Medium | ✅ Yes (fs mocking) |
| 2 | isDBBusy() unit tests | — | 10m | Low | ✅ Yes (direct logic) |
| 3 | Maintain() error paths | 3 | 20m | Low | ✅ Yes (db mocking) |
| 4 | ChronicleReindex() | 168 | 120m | High | ✅ Yes (PTY mock) |
| 5 | findCopilotBinary() | 63 | 60m | Medium | ✅ Yes (fs + env mock) |
| 6 | startPTY() error paths | 14 | 20m | Low | ✅ Yes (fn mock) |

